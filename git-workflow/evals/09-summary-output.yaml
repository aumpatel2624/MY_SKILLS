name: summary-output
description: Skill closes with a clear structured summary
type: encoded_preference

prompt: "help me with git — I refactored the database connection pool"

expected:
  must_match:
    - "(branch name|Branch)"
    - "(commit message|Commit)"
    - "(CHANGELOG|changelog)"
    - "(command|Command)"

tags: [summary, output, core]
